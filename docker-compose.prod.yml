services:
  piper-tts:
    image: ghcr.io/crazy-goat/piper-php-api:latest
    ports:
      - "8000:8000"
    volumes:
      - models-data:/app/models
    restart: unless-stopped

volumes:
  models-data:
    driver: local
